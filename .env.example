AUTOMATION_DIAGNOSIS_WEBHOOK_URL=https://n8n.vektrum.agency/webhook/inbound/automation-diagnosis

# Newsletter ("Semana em IA") subscribe/unsubscribe — same Supabase project
# as vektrum-crm (Project Settings → API on the Vektrum Supabase project).
# Service role, not anon: newsletter_subscribers has no anon/authenticated
# grants (see vektrum-crm/supabase/migrations/20260903120000_newsletter.sql),
# and this key is only ever read server-side (lib/supabase/server.ts).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
